= Journal des modifications
:toc: left
:toc-title: Versions
:toclevels: 2

== v0.1.5 — 2026-06-05

=== Modifié

* **Le focus de `help <sub>` est désormais cantonné à la section.** Le
  filtre `help <sub>` (ajouté en v0.1.4) parcourait tout le banner USAGE
  et pouvait tomber sur une ligne antérieure où le nom de la
  sous-commande apparaît (ligne d'usage, description d'une option). Il ne
  commence maintenant à chercher qu'à l'intérieur de la section
  « Sous-commandes : », donc `noto-cjk help pull` pointe de façon fiable
  sur l'entrée de la sous-commande `pull`.

== v0.1.2 — 2026-04-29

=== Modifié

* **Module interne renommé** `CrystalNotoCJK` → `NotoCjk` (convention
  Aloli : retrait du préfixe `Crystal` et CamelCase strict — pas de
  préservation d'acronyme, pour cohérence dans tout l'écosystème).
* Dossier source `src/crystal_noto_cjk/` → `src/noto_cjk/` et entrée
  consolidée dans `src/noto-cjk.cr` (point d'entrée canonique
  correspondant à `require "noto-cjk"`). Chemins `require` internes
  adaptés.
* Binaire CLI inchangé.

=== Note pour les consommateurs

Le code externe qui référençait `CrystalNotoCJK::*` (par ex.
`crystal-asciidoctor-pdf`) doit être mis à jour vers `NotoCjk::*` et
re-épinglé sur `~> 0.1.2`.

== v0.1.1 — 2026-04-29

=== Modifié

* **Repo GitHub renommé** `aloli-crystal/noto-cjk` →
  `aloli-crystal/noto-cjk` (convention de nommage Aloli : les
  créations originales abandonnent le préfixe `crystal-`). Les anciens
  liens redirigent automatiquement.
* Champ `name:` du `shard.yml` mis à jour de `noto-cjk` à `noto-cjk`.
* Cible de compilation et binaire CLI renommés `noto-cjk` →
  `noto-cjk` (`shards build noto-cjk` → `bin/noto-cjk`).
* Nouveau fichier d'entrée shim `src/noto-cjk.cr` (charge
  `src/crystal_noto_cjk.cr`) ; `require "noto-cjk"` devient
  `require "noto-cjk"`.

== v0.1.0 — 2026-04-26

Première version publique. Couverture des glyphes CJK pour les
projets Crystal via téléchargement à la demande des polices
Noto Sans CJK.

* Module **`CrystalNotoCJK::Cache`** : téléchargement par
  variante, inspection et purge.
* **API** : `Cache.pull(variants:, source:, system:)`,
  `Cache.path(variant)`, `Cache.populated?(variant)`,
  `Cache.installed`, `Cache.purge`.
* **Wrappers haut niveau** : `CrystalNotoCJK.font_path`
  (n'importe quelle variante installée),
  `CrystalNotoCJK.font_path(:jp)` (spécifique).
* **CLI** `bin/noto-cjk` avec sous-commandes `pull`,
  `info`, `purge` et drapeaux courts/longs `-V/--variant`,
  `-s/--source`, `--system`, `-v/--version`, `-h/--help`.
* **Quatre variantes supportées** : `:sc` (Chinois Simplifié,
  défaut), `:tc` (Chinois Traditionnel), `:jp` (Japonais),
  `:kr` (Coréen), ou `--variant all` pour tout.
* **Support des miroirs privés** via `--source URL` — pointer
  vers n'importe quel serveur de fichiers statiques qui
  reproduit l'arborescence amont `Sans/OTF/`.
* **Chemin XDG conventionnel** avec variante macOS +
  override `--system` + variable d'env
  `CRYSTAL_NOTO_CJK_CACHE_DIR`.
* **Idempotent** — relancer `pull` sur des variantes déjà en
  cache est sans effet.
* **Téléchargements atomiques** — tmp puis rename, donc un
  téléchargement partiel ne laisse jamais d'OTF corrompu dans
  le cache.
* **Aucun payload embarqué** — le shard lui-même est minuscule
  (pas de polices livrées). Chaque variante fait ~16-20 Mo et
  n'est téléchargée que lorsque l'utilisateur le demande
  explicitement.
* **MIT** pour le code Crystal ; les polices en cache
  conservent la licence SIL OFL v1.1 de Noto.
* README bilingue (EN + FR), CI, lint ameba.
* **12 tests unitaires, tous verts** (couverture des chemins
  sans réseau — le `pull` réel n'est intentionnellement PAS
  exercé en CI pour éviter de tirer ~16 Mo à chaque run).
